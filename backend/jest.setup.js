// Jest setup for security and validation tests

// Suppress console outputs during tests unless explicitly testing them
global.console = {
  ...console,
  log: jest.fn(),
  warn: jest.fn(),
  error: jest.fn(),
  info: jest.fn(),
  debug: jest.fn(),
};